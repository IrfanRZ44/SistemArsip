<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/auth_nav"
    android:label="Authentication"
    tools:showIn="navigation_view"
    app:startDestination="@id/splashFragment">
    <fragment
        android:id="@+id/nav_beranda"
        android:name="com.exomatik.sistemarsip.ui.main.beranda.BerandaFragment"
        android:label="@string/beranda"
        tools:layout="@layout/fragment_beranda" />
    <fragment
        android:id="@+id/splashFragment"
        android:name="com.exomatik.sistemarsip.ui.main.SplashFragment"
        android:label="@string/splash_screen"
        tools:layout="@layout/fragment_splash" />
</navigation>